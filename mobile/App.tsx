// Entry point. Navigation stack is wired here once screens exist —
// see CLAUDE.md Section 2 for the MVVM layering (View / ViewModel / Service)
// this app follows.

import { View, Text } from "react-native";

export default function App() {
  return (
    <View>
      <Text>Tahqiq — scaffold</Text>
    </View>
  );
}
